/*
 *
 * Constants: `@exsys-patient-insurance/{{name}}`
 *
 */

//REPLACE col_1 WITH TABLE PRIMARY KEY;
export const rowKey = "col_1";

// Insert table record data as key value pair without total and record primary key attributes
export const defaultNewRecord = Object.freeze({
  record_status: "q"
});

/*
 *
 * Modal View
 *
 */
import { memo } from "react";
import { useTableContext } from "@exsys-patient-insurance/table";
import Modal from "@exsys-patient-insurance/modal"
import { Flex } from "@exsys-patient-insurance/commons";

const ModalView = () => {
  const {
    onInputChange,
    selectedRowData,
    modalRef,
    setModalClosed,
    onSave,
    isAddOrUpdateLoading
  } = useTableContext();

  return (
      <Modal
        ref={modalRef}
        title="dtls"
        width="500px"
        onOk={onSave}
        disabled={isAddOrUpdateLoading}
      >
        <Flex>Modal View</Flex>
      </Modal>
  );
};

export default memo(ModalView);

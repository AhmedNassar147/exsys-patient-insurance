### `Please Always read the interfaces files if any of current package to see it's properties`.

## Description:

```sh
  React component to render a text.
```

## Features:

```sh
  A) provides a text component that can render a translated children if given children was string
  # else it render the given children;
```

## Usage Examples.

```sh
  [A](some usage link)
```
